- To run the dev server use `bun run start:dev:portless`.
- To get the current checkout/worktree URL after `bun run start:dev:portless`, run `portless get runtipi` from this directory.
- If `portless get runtipi` does not return a route, run `portless list` and use the `runtipi` route for this worktree.

- Credentials :
  - Username: test@test.com
  - Password: password
