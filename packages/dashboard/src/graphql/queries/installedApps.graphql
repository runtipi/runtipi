query InstalledApps {
  installedApps {
    id
    status
    config
    version
    updateInfo {
      current
      latest
    }
    info {
      id
      name
      description
      tipi_version
      short_desc
    }
  }
}
